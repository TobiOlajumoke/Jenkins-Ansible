---
- name: update web and nfs 
  hosts: webservers, nfs
  remote_user: ec2-user
  become: yes
  become_user: root
  tasks:
    - name: ensure wireshark is at the latest version
      yum:
        name: wireshark
        state: latest

- name: update LB server and DB server
  hosts: lb, db
  remote_user: ubuntu
  become: yes
  become_user: root
  tasks:
    - name: Update apt repo
      apt: 
        update_cache: yes

    - name: create folder and file
      file:
        path: /home/ubuntu/tob/bankai
        state: touch
        mode: 0644
        owner: ubuntu
        group: ubuntu
      lineinfile:
        path: /home/ubuntu/tob/bankai
        line: 'KUROSAKI ICHIGO'
        insertbefore: BOF

    - name: ensure wireshark is at the latest version
      apt:
        name: wireshark
        state: latest
